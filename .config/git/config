[user]
	name = Cory Hernandez
	email = cory.hernandez@alteryx.com
[push]
	autoSetupRemote = true
[alias]
	reboot = "!f() { git switch main || return; git for-each-ref --format=\"%(refname:short)\" refs/heads/ | while IFS= read -r branch; do [ \"$branch\" = main ] && continue; git branch -D -- \"$branch\"; done; }; f"
